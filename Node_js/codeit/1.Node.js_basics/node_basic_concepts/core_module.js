// * core module - example
// const fs = require("fs");

// let fileList = fs.readdirSync(".");
// console.log(fileList);

// fs.writeFileSync("new.js", "Hello Node.js!");
const os = require("os");

console.log(os.cpus());
